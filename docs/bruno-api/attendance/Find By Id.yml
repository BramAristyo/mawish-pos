info:
  name: Find By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/attendances/{{attendance_id}}"
  auth: inherit

vars:
  pre-request:
    attendance_id: 00000000-0000-0000-0000-000000001801

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
