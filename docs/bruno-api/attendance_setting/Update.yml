info:
  name: Update
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/attendance-settings"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "lat": -6.200000,
        "lng": 106.816666,
        "radius": 100.0
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
